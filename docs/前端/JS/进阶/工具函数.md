# 工具函数



```javascript
export function getUrlQueryString(obj) {
  return Object.keys(obj)
    .map((key) => `${key}=${obj[key]}`)
    .join('&')
}

const url = `/edu-plan-v2/ai-personnel-training-assistant/execute/workflow?${getUrlQueryString(payload.query)}`
```

