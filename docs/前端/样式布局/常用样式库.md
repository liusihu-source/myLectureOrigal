# 常用样式库



## 字体引入

```css
@font-face{font-family:'PangMenZhengDao';src:url(../css/fonts/庞门正道标题体2.0增强版.ttf);}

.s_itemTitle span {
  font-family: 'PangMenZhengDao';
}
```



## 文字渐变

```
<h1>AI智能选课推荐</h1>
```

```css
.s_learning_recommend_titleCon h1{
    font-size: 18px; 
    font-weight: bold; 
    background: linear-gradient(90deg, #4D59FF 0%, #0A8DFF 100%);
    background-clip: text;
    -webkit-text-fill-color: transparent;
}
```



## 悬浮提示

```css
/* 气泡容器 */
    .tooltip-container {
      position: relative;
      display: inline-block;
      margin: 50px;
    }

    /* 触发元素 */
    .tooltip-trigger {
      background: #007bff;
      color: white;
      padding: 8px 16px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    /* 气泡内容（默认隐藏） */
    .tooltip-bubble {
      visibility: hidden;
      background-color: #333;
      color: #fff;
      text-align: center;
      border-radius: 4px;
      padding: 8px 12px;
      position: absolute;
      z-index: 1;
      bottom: 125%; /* 气泡在按钮上方 */
      left: 50%;
      transform: translateX(-50%);
      white-space: nowrap;
      opacity: 0;
      transition: opacity 0.3s, visibility 0.3s;
    }

    /* 气泡小三角 */
    .tooltip-bubble::after {
      content: "";
      position: absolute;
      top: 100%;
      left: 50%;
      margin-left: -5px;
      border-width: 5px;
      border-style: solid;
      border-color: #333 transparent transparent transparent;
    }

    /* 鼠标悬停时显示气泡 */
    .tooltip-container:hover .tooltip-bubble {
      visibility: visible;
      opacity: 1;
    }
```

```html
<div class="tooltip-container">
    <button class="tooltip-trigger">悬停我</button>
    <div class="tooltip-bubble">这是一个气泡提示</div>
</div>
```




## 滚动条

`s_custemScroll`

```css
.s_custemScroll::-webkit-scrollbar{
  width: 8px;  /* 滚动条宽度 */
  background: transparent;  /* 背景透明 */
}
/* 滚动条滑块样式 */
.s_custemScroll::-webkit-scrollbar-thumb{
  background: #DDDCE5;  /* 半透明滑块 */
  border-radius: 4px;
}
/* 鼠标悬停时滑块更明显 */
.s_custemScroll::-webkit-scrollbar-thumb:hover{
  background: rgba(0, 0, 0, 0.5);
}
```

![image-20260108171056512](./%E5%B8%B8%E7%94%A8%E6%A0%B7%E5%BC%8F%E5%BA%93.assets/image-20260108171056512.png)







## 单（多）行显示

`s_singleLine` `s_multipleLine2`

```css
/*单行*/
.s_singleLine {
    display: inline-block; /* 或 block */
    max-width: 125px;
    white-space: nowrap; /* 禁止换行 */
    overflow: hidden;
    text-overflow: ellipsis;
}
```

```css
/*多行*/
.s_multipleLine2 {
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
    overflow: hidden;
    text-overflow: ellipsis;
}
```



## 自定义属性（CSS变量）

```vue
<template>
  <div class="wrap" :style="cssVars">
    <div class="box"></div>
  </div>
</template>

<script setup>
import { reactive } from 'vue'
const state = reactive({
  size: 24,
  boxWidth: 200,
  color: '#67c23a'
})
const cssVars = {
  '--size': `${state.size}px`,
  '--box-width': `${state.boxWidth}px`,
  '--theme-color': state.color
}
</script>

<style scoped>
.box {
  width: var(--box-width);
  height: var(--size);
  font-size: var(--size);
  background: var(--theme-color);
}
</style>
```







## -----------------------

## 自定义复选框样式 - 公司

`s_customCheck (雪碧图)`

```
<div class='s_customCheck'>
   <input type="checkbox" id="select1" v-model="select1" @click="myChose" />
   <label for="select1"></label>
</div>
```

```css
.s_customCheck input {
    display: none;
}
.s_customCheck label {
    display: inline-flex;
    align-items: center;
    color: #131B26;
    font-size: 14px;
    line-height: 36px;
    cursor: pointer;
}
.s_customCheck label::before {
    margin-right: 10px;
    content: '';
    display: inline-block;
    width: 18px;
    height: 18px;
    background: url(/img/ico-engine-check.png) no-repeat 0 0 / 100% auto;
}
.s_customCheck input:checked + label::before{background-position:0 -20px;}
```

![image-20260109113518370](./%E5%B8%B8%E7%94%A8%E6%A0%B7%E5%BC%8F%E5%BA%93.assets/image-20260109113518370.png)


## 单选组  - 公司
```css
.radio-group {
    display: flex;
    align-items: center;
    font-family: "Helvetica Neue", Arial, sans-serif;
    margin: 20px;
}
.radio-option {
    display: flex;
    align-items: center;
    margin-right: 40px;
    cursor: pointer;
    user-select: none;
}
.radio-custom {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    border: 1.5px solid #e0e0e0;
    margin-right: 10px;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
}
.radio-option input[type="radio"] {
    opacity: 0;
    width: 0;
    height: 0;
}
.radio-option input[type="radio"]:checked + .radio-custom {
    border-color: #4080ff;
}
.radio-option input[type="radio"]:checked + .radio-custom::after {
    content: "";
    width: 14px;
    height: 14px;
    border-radius: 50%;
    background-color: #4080ff;
}
.radio-label {
    font-size: 16px;
    color: #333;
}

```

```html
<div class="radio-group">
  <label class="radio-option">
    <input type="radio" name="gender" value="male" checked>
    <span class="radio-custom"></span>
    <span class="radio-label">先生</span>
  </label>
  
  <label class="radio-option">
    <input type="radio" name="gender" value="female">
    <span class="radio-custom"></span>
    <span class="radio-label">女士</span>
  </label>
</div>
<script>
  document.querySelector('.radio-group').addEventListener('change', (e) => {
      if (e.target.name === 'gender') {
          console.log('选中了:', e.target.value);
      }
  });
</script>
```
![image-20260116105700016](./%E5%B8%B8%E7%94%A8%E6%A0%B7%E5%BC%8F%E5%BA%93.assets/image-20260116105700016.png)





## ---------------------------------------------------------------



## 内置组件transition 

- 只要包裹内容隐藏/显示，就会触发对应样式
- 默认只能包含一个子项

```vue
<script setup lang="ts">
import { ref } from 'vue'
const show = ref(false)
</script>

<template>
  <div class="container">
    <transition name="fade">
      <div class="box" v-if="show"></div>
    </transition>
    <button @click="show = !show">{{ show ? '隐藏' : '显示' }}</button>
  </div>
</template>

<style scoped>
.box {
  width: 100px;
  height: 100px;
  background: #22c55e;
}

.fade-enter-active,
.fade-leave-active {
  transition: opacity 0.5s ease;
}

.fade-enter-from,
.fade-leave-to {
  opacity: 0;
}
</style>

```

