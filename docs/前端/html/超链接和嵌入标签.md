# 超链接和嵌入

## 超链接 a



## 嵌入 iframe

```
<iframe
  src="https://example.org"  //嵌入网页
  title="iframe Example 1" //描述信息
  width="400" 
  height="300"
  allowfullscreen //是否全屏
  sanbox //沙盒模式
>
</iframe>
```



## src 和 href

### src

- 指定外部资源引用路径；
- 用于 img，script，iframe 等标签中；
- 浏览器解析具有 src 属性的标签，立刻下载并加载；

### href

- 指定超链接的引用路径；
- 用于 a，link 等标签中；
- 浏览器解析到具有 href 属性的标签；
  - a 会跳转至制定 url；
  - link 会引用外部资源



当前页面下载文件

```javascript
function downloadFile(url) {
  const iframe = document.createElement('iframe')
  iframe.style.display = 'none'
  iframe.src = url
  document.body.appendChild(iframe)
  
  // 下载完成后移除iframe
  setTimeout(() => {
    document.body.removeChild(iframe)
  }, 1000)
}

// 下载文件
const handleDownload = (url) => {
  downloadFile(url)
  // downloadFile(url)
}
```

