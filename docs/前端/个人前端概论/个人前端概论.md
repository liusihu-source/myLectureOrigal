# 个人前端概论

## 一、手册

地址：https://developer.mozilla.org/zh-CN/

## 二、编辑器

1. IDE系列：
   - webstorm

2. 文本编辑器系列：
   - vscode
   - cursor
   - trae

## 三、关系

1. 超文本标记语言（HTML）：
   - 骨架

2. 层叠样式表（CSS）：
   - 形象

3. 文档对象模型（DOM）：
   - **定义：**`document`对象 ，树状结构，每个树节点为一个DOM标签，通过JS交互
4. **浏览器对象模型（BOM）：**
   - **定义：** 浏览器本身的编程接口。如 `window`、`history`、`location` 和 `navigator`。通过JS交互
5. **JS：**
   - The Core (ECMAScript)、操控DOM、交互BOM
