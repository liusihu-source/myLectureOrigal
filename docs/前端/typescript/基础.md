# 基础

## 一、数据类型

### 1.1 原始数据类型

```typescript
// 布尔值
const isDone: boolean = false;
// 数值
const num: number = 1;
// 字符串
const myName: string = "Tom";
// 表示函数返回空
const alertName = (): void => {
  console.log("My name is Tom");
};
// undefined 和 null 是任何类型的子类型
// undefined 表示变量没有值得初始状态;
const u: undefined = undefined;
// 表示什么都没有
const n: null = null;
```



### 1.2 其他类型

#### any

- 表示可以赋予任何类型, 可以使用任何类型的属性和方法

```typescript
let anyThing: any = "Tom";
```



#### unknown

- 表示不知道类型的变量，需要使用类型断言确定类型

```typescript
function isUser(obj: unknown): obj is { name: string; age: number } {
  return (
    typeof obj === 'object' &&
    obj !== null &&
    'name' in obj &&
    'age' in obj &&
    typeof (obj as any).name === 'string' &&
    typeof (obj as any).age === 'number'
  );
}
```



#### never

- never 是任何值的子类型
- never 表示永远不存在值的类型
- 函数不会返回值, 抛出异常时, 返回 never

```typescript
// ✅ 有 never 检查 - 编译时报错
type Status = 'loading' | 'success' | 'error';

function handleStatus(status: Status) {
  switch (status) {
    case 'loading': console.log('加载中'); break;
    case 'success': console.log('成功'); break;
    // case 'error': console.log('错误'); break;  //取消这行注释就能编译通过
  }
  const _: never = status;  // ❌ 编译错误！
  // 错误：Type '"error"' is not assignable to type 'never'， 提示你忘记处理 'error' 情况
}
```

:bulb: never 只是用于`编译时检查问题`，确保各个情况都考虑到，启动后不受影响

:bulb: `TypeScript 能推导出代码执行路径`，知道哪些分支可能执行 , 开发时的安全网



#### void

- 表示没有任何类型

```typescript
function warnUser(): void {
  console.log("This is my warning message");
}
```



### 1.3 接口

#### 定义接口

```typescript
interface Person {
  name: string;
  age: number;
}
```



#### 可选属性

```typescript
interface Person {
  name: string;
  age?: number;
}
```



#### 任意数量属性

- 确定属性和可选属性必须为任意属性的子类型

```typescript
interface RequestParams {
  method: 'GET' | 'POST';
  url: string;
  [param: string]: string | number;  //允许对象拥有任意数量的额外属性，只要属性名是 string类型，属性值是 string | number类型
}
```



#### 只读属性

```typescript
interface Person {
  readonly id: number;
  name: string;
  age?: number;
  [propName: string]: any;
}
```



### 1.4 函数类型

#### 语法格式

```typescript
function (x: number, y: number): number {
    return x + y;
};
const sum = (x: number, y: number): number => {
  return x + y;
};
```



#### 可选类型

- 可选参数位于必选参数后

```typescript
const buildName = (firstName: string, lastName?: string) => {
  // ...
};
```



#### 函数默认值

```typescript
const buildName = (firstName: string, lastName: string = "kong") => {
  // ...
};
```



#### 任意数量参数

- 位置位于最后

```typescript
const push = (array: any[], ...items: any[]) => {
  //...
};
```



#### 函数重载

- 同一个函数接受不同数量或类型的参数

```typescript
function reverse(x: number): number;
function reverse(x: string): string;
function reverse(x: number | string): number | string | void {
  if (typeof x === "number") {
    return Number(x.toString().split("").reverse().join(""));
  } else if (typeof x === "string") {
    return x.split("").reverse().join("");
  }
}
```



#### 实现箭头函数重载

- 使用 interface 和 as 实现箭头函数重载;

```typescript
interface FN {
  (x: number): number;
  (x: number, y: number): number;
}
const reverse1: FN = (x: number, y?: number): number => {
  if (y) {
    return x + y;
  } else {
    return x;
  }
};

const reverse2 = ((x: number, y?: number): number => {
  if (y) {
    return x + y;
  } else {
    return x;
  }
}) as FN;  // 用于确保类型 告诉TS编译器放宽检查
```



### 1.5 联合类型和交叉类型

#### 联合类型

- 联合类型表示几个类型中的一个;
- 只能访问联合类型共有的方法和属性;

```typescript
let myFavoriteNumber: string | number;
myFavoriteNumber = "seven";
myFavoriteNumber = 7;
```



#### 交叉类型

- 交叉类型将多个类型合并在一起;
- 具有多个类型的属性和方法;

```typescript
interface ErrorHandling {
  success: boolean;
  error?: { message: string };
}

interface ArtworksData {
  artworks: { title: string }[];  //对象数组，对象必须有title属性，且是string
}
type ArtworksResponse = ArtworksData & ErrorHandling;
```



### 1.6 字面量类型

```typescript
// 约束取值只能为规定值中的一个;
type EventNames = "click" | "scroll" | "mousemove";
const handleEvent = (ele: Element, event: EventNames) => {
  // do something
};
handleEvent(document.getElementById("hello"), "scroll"); // 没问题
handleEvent(document.getElementById("world"), "dblclick"); // 报错, event 不能为 'dblclick'
```



### 1.7 数组类型

```typescript
// 类型 + 方括号
let fibonacci: number[] = [1, 1, 2, 3, 5];
let fibonacci: object[] = [1, 1, 2, 3, 5];
// 泛型
let fibonacci: Array<number> = [1, 1, 2, 3, 5];
// 接口
interface NumberArray {
  [index: number]: number;
}
let fibonacci: NumberArray = [1, 1, 2, 3, 5];
```



### 1.8 元祖

