### prompt - 静态页

```
请使用Figma MCP,将我选中的 Figma 设计稿**1:1 精准还原**为 Vue3 静态页面，存放到index.html中。figma链接：https://www.figma.com/design/MIQGEU1EkwUgu8ZUNZPPEr/%E7%BA%BF%E4%B8%8B%E5%9F%B9%E8%AE%AD%E5%B9%B3%E5%8F%B0?node-id=926-6042&t=EJGv1Udw6i1X0jeI-4
要求如下：

1. vue3引入 /js/vue.global.js文件,使用 Vue3 + 组合式 API + <script setup> 语法
2. 生成的css存到 /css/index.css文件中,使用原生 CSS，所有css样式名前边都用s_前缀
3. 代码结构清晰、语义化 HTML、可直接运行
4. 完全复刻：宽度、高度、内边距、外边距、位置、层级(z-index)
5. 颜色：直接使用 Figma 取色值（十六进制 / rgba）
6. 字体：字号、字重、行高、字体家族完全与设计一致
7. 效果：阴影、圆角、透明度、渐变完全还原
8. 布局：使用 Flex 实现与设计相同的排版结构
9. 涉及的图片 svg可以留出空位，后续我自己添加
10. 交互逻辑内部不处理，只添加方法名及方法名的方法说明即可，最后输出静态页面结构与样式
```

### prompt - 样式

```
不要修改任意样式，只帮我改格式要求如下
1.不要留任何空白行
2.所有样式都直接一行下，如 .s_course_card.locked .s_course_title h4 {color: #999;}，不要换行写，，直接给我最终结果
```

### prompt - 图表

```

```

