# 编程思想

## 函数式编程

### 基础函数

- 关注程序的输入和输出;
- 忽略程序的过程;
- 将程序的执行过程抽象为一个函数;



#### 纯函数

- 数据不可变 + 无状态性 + 固定输入-输出;
  - 无状态性: 不依赖也不会改变外部作用域的变量;
  - 数据不可变: 函数不会改变输入数据;
  - 固定输入-输出: 相同的输入必然是相同的输出;

```javascript
function calc3Plus4 (a, b) {
    return 3 + 4
}
```



#### 副作用函数

- 函数本身进行与函数返回值无关的操作, 即修改外部状态
  - 发送网络请求;
  - 修改 DOM;
  - 本地存储;
  - 状态管理;



### 柯里化函数

把接收多个参数的函数 转变成 接收单一参数（最初函数的第一个参数）的函数，并且返回接收余下参数而且返回结果的新函数。

```javascript
function sumMaker(length){
    let nums = []
    function sum(...args){
        nums.push(...args)
        if(nums.length >= length){
            const res = nums.slice(0,length).reduce((p,v) => p + v, 0)
            nums = []
            return res
        } else{
            return sum
        }
    }
    return sum
}

//测试
const sum4 = sumMaker(4)
console.log(sum4(1,2)(3)(4))  // 10

const sum6 = sumMaker(6)
console.log(sum6(1,2)(3)(4,5)(6))  //21
```



### 组合和管道

#### 组合

- 将多个函数组合成一个函数;
- 从右向左执行他的组成函数, 并将上一个的输出作为下一个的输入;

```javascript
const compose = (...fns) => {
  return (x) => {
    return fns.reduceRight((beforeRes, fn) => {
      return fn(beforeRes);
    }, x);
  };
};

const addOne = (x) => x + 1;
const double = (x) => x * 2;
const subtractThree = (x) => x - 3;

const composedFunc = compose(subtractThree, double, addOne);
console.log(composedFunc(3)); // 5
```

#### 管道

- 将多个函数组合成一个函数;
- 从左向右执行他的组成函数, 并将上一个的输出作为下一个的输入;

```javascript
const pipe = (...fns) => {
  return (x) => {
    return fns.reduce((beforeRes, fn) => {
      return fn(beforeRes);
    }, x);
  };
};

const addOne = (x) => x + 1;
const double = (x) => x * 2;
const subtractThree = (x) => x - 3;
const pipedFunc = pipe(addOne, double, subtractThree);
console.log(pipedFunc(3)); // 5
```



### 函数缓存

- 基于柯里化、闭包 和 哈希表实现函数缓存

```javascript
const memoize = function (fn, context) {
  let cache = Object.create(null);
  context = context || window;
  return (...args) => {
    if (!cache[args]) {
      cache[args] = fn.apply(context, args);
    }
    return cache[args];
  };
};

const add = (a, b) => a + b;
const calc = memoize(add);
const num1 = calc(100, 200);
const num2 = calc(100, 200); // 缓存得到的结果
```



## API设计

### axios请求

- axios配置

```javascript
import axios from 'axios'

const FILE_TYPE = [
  'image/jpeg', // JPG图片
  'image/jpg', // JPG图片
  'image/png', // PNG图片
  'image/bmp', // BMP图片
  'image/webp', // WEBP图片
  'image/svg+xml', // SVG图片
  'image/tiff', // TIFF图片
  'image/gif', // GIF图片
  'application/octet-stream', // 二进制流文件
  'application/pdf', // PDF文件
  'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet', // XLSX文件
  'application/ms-excel', // XLSX文件
  'application/vnd.ms-excel', // XLS文件
  'application/vnd.openxmlformats-officedocument.wordprocessingml.document', // DOCX文件
  'application/msword', // DOC文件
  'application/vnd.ms-word' // DOC文件
]

function request(options) {
  return new Promise((resolve, reject) => {
    // 请求头
    const headers = {
      'Content-Type':
        options.method === 'post'
          ? 'application/x-www-form-urlencoded; charset=UTF-8'
          : 'application/json; charset=UTF-8',
      ...options.config?.headers
    }
    // 创建axios实例
    const service = axios.create({
      baseURL: options.url.includes('http') ? options.url : import.meta.env.VUE_APP_baseUrl, // baseURL将会被加在url前面。
      timeout: 10000, // 超时
      withCredentials: options.config?.withCredentials ?? true, // 是否允许带cookie这些
      processData: true,
      ...options.config,
      headers
    })

    // request拦截器
    service.interceptors.request.use(
      async (config) => {
        return config
      },
      (error) => {
        Promise.reject(error)
      }
    )

    // 响应拦截器
    service.interceptors.response.use(
      (res) => {
        const handleErrCode = res.data.code
        if (
          res.status === 200 ||
            handleErrCode === '10000' ||
          res.data.status === true ||
          res.data.success === true ||
          res.data.result === true ||
          res.data.status === '1'
        ) {
          resolve(res.data)
        } else if (FILE_TYPE.filter((i) => res.headers['content-type'].includes(i)).length) {
          resolve(res.data)
        } else {
          reject(res.data)
        }
      },
      (error) => {
        let { message } = error

        if (message == 'Network Error') {
          message = '网络异常，请检查网络'
        } else if (message.includes('timeout')) {
          message = '系统接口请求超时，请检查网络'
        } else if (message.includes('Request failed with status code')) {
          message = '系统接口' + message.substr(message.length - 3) + '异常'
        }

        return reject(error)
      }
    )

    service(options)
  })
}

export function httpPost(url, data, config = {}) {
  return request({
    url: url,
    method: 'post',
    timeout: 100000,
    data,
    config
  })
}

export function httpGet(url, params, config = {}) {
  return request({
    url: url,
    method: 'get',
    timeout: 100000,
    params,
    config
  })
}
```

- 防腐层API统一管理

```javascript
import { httpGet, httpPost } from '@/api/http'

const getUrlQueryString = (obj) => {
  return Object.keys(obj)
    .map((key) => `${key}=${obj[key]}`)
    .join('&')
}

export function getIndustryJobAnalysisDataFetch(payload) {
  let url = '/xxxxxx'
  return new Promise((resolve, reject) => {
    httpGet(url, payload)
      .then((res) => {
        resolve(res)
      })
      .catch((err) => {
        reject(err)
      })
  })
}

export function saveIndustryJobAnalysisDataFetch(payload) {
  let url = `/xxxxxx?${getUrlQueryString(payload.query)}`
  return new Promise((resolve, reject) => {
    httpPost(url, payload.params, {
      processData: false,
      headers: { 'Content-Type': 'application/json' }
    })
      .then((res) => {
        Toast({
          msg: '11111',
          type: 'success'
        })
        resolve(res)
      })
      .catch((err) => {
        Toast({
          msg: '由于系统问题，xxx失败，请间隔一段时间后xxxx',
          type: 'error'
        })
        reject(err)
      })
  })
}
```

- 实际使用

```javascript
saveIndustryJobAnalysisDataFetch({
    query: {
      fid: params.fid,
      planId: params.planId,
      planIdEnc: params.planIdEnc
    },
    params:{
      data: data,
      mode: type
    }
  }).then(res => {
    console.log(res)
  })
```



### Fetch请求

```javascript
const getUrlQueryString = (obj) => {
  return Object.keys(obj)
    .map((key) => `${key}=${obj[key]}`)
    .join('&')
}

function isValidJSON(str) {
  if (typeof str !== 'string') return false
  try {
    JSON.parse(str)
    return true
  } catch (e) {
    return false
  }
}

export async function streamWorkFlowFetch(payload, returnData, errorHandle) {
  const headers = {
    'Accept': '*/*',
    'Content-Type': 'application/json'
  }
  const pattern = /data:(.*?)(?=data:|$)/gs // 修改正则表达式
  function parseStreamLine(line) {
    const matches = [...line.matchAll(pattern)]
    const content = matches.map((match) => {
      return match[1].replace(/(?:\n\n)$/g, '') // 只替换末尾的 \n\n
    })
    return content
  }
  try {
    const url = `/xxxx?${getUrlQueryString(payload.query)}`
    const response = await fetch(url, {
      method: 'post',
      headers: headers,
      body: JSON.stringify(payload.params)
    })
    const reader = response.body.getReader()

    // 针对流片段进行缓存，如果是多个json片段的话要进行合并之后在返回
    let tempJson = ''
    while (true) {
      const { done, value } = await reader.read()
      if (done) {
        returnData('', done)
        break
      }
      let newText = new TextDecoder('utf-8').decode(value)
      if (!newText.startsWith('data:')) {
        newText = 'data:' + newText
      }
      if (!newText.endsWith('\n\n')) {
        newText += '\n\n'
      }
      const obj = parseStreamLine(newText)
      obj.forEach((i) => {
        // 如果当前是一个合法json直接返回
        if (isValidJSON(i)) {
          returnData(i, false)
        } else {
          tempJson += i
          if (isValidJSON(tempJson)) {
            returnData(tempJson, false)
            tempJson = ''
          }
        }
      })
    }
  } catch (e) {
    errorHandle(e)
  }
}

```

- 实际使用

```javascript
streamWorkFlowFetch(
  {
    query: {
    },
    params: {
    }
  },
  (data, done) => {console.log(data)},
  (e) => {console.log(e)
} 
```



## 面相对象

### 封装性

- 对象方法和属性包装在对象内部;
- 外界无法直接访问对象内部, 隐藏对象内部细节;
- 对外提供固定接口;

### 继承性

- 子类继承父类, 进而复用父类代码;
- 子类可以重载父类;

### 多态性

- 同一操作作用于不同对象, 产生不同的结果;
- 可通过组合加委托, 在运行时中传入不同接口实现, 实现多态性;



## 前端工程化

