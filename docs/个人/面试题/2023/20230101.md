# 前端面试题

## 2023.01.01

### 1.watch

- 在不添加immediate：true时，默认不监听第一次赋值

### 2.scoped

- 父组件样式不会渗透到子组件，但子组件会同时受到父 和 子组件的css影响
- 可通过 >>> 或者 :deep 实现样式穿透
- scoped原理是在标签上添加data-v属性，后使用属性选择器实现样式局部化

### 3.v-model number

- 添加number修饰符后，赋值如果无法被parseFloat()解析，则返回原始的值

```html
<input v-model.number = 'age'>
```

### 4.循环

- for ... in 拿到迭代对象的下标
- for ... of 拿到迭代对象的属性

### 5.RegExp（Regular Expression）

- 正则表达式
- 有方法exec用于检索字符串中正则表达式的匹配

### 6.变量及函数声明及赋值顺序

```javascript
var foo=function(x,y){
	return x-y;
}
function foo(x,y){
	return x+y;
}
var num=foo(1,2);
```

实际执行顺序如下

```javascript
var foo;
var num;
function foo() {
	return x + y
}
foo = function(){
	return x - y
}
num = foo(1,2)
```

### 7.空字符串false

```javascript
<script LANGUAGE="JavaScript"> 
	var a="undefined";
    var b="false";
    var c="";
    function assert(aVar){
        if(aVar)      
            alert(true);
        else  
            alert(false);
    }
    assert(a);
    assert(b);
    assert(c); </script>
```

true, true, false


## 2023.01.02

### 1.JS相等性判断

| x                 | y                 | ==    | ===   |
| ----------------- | ----------------- | ----- | ----- |
| underfined        | underfined        | true  | true  |
| null              | null              | true  | true  |
| true              | true              | true  | true  |
| false             | false             | true  | true  |
| "foo"             | "foo"             | true  | true  |
| 0                 | 0                 | true  | true  |
| +0                | -0                | true  | true  |
| 0                 | false             | true  | false |
| ""                | false             | true  | false |
| ""                | 0                 | true  | false |
| "0"               | 0                 | true  | false |
| "17"              | 17                | true  | false |
| [1,2]             | "1,2"             | true  | false |
| new String("foo") | "foo"             | true  | false |
| null              | underfined        | true  | false |
| null              | false             | false | false |
| underfined        | false             | false | false |
| {foo: "bar"}      | {foo: "bar"}      | false | false |
| new String("foo") | new String("foo") | false | false |
| 0                 | null              | false | false |
| 0                 | NaN               | false | false |
| "foo"             | NaN               | false | false |
| NaN               | NaN               | false | false |

## 2023.01.03

### 1.this

```javascript
let color = 'green'
const test = {
	color: 'blue',
	getColor: function () {
		var color = 'red'
		alert(this.color)
	}
}
const getColor = test.getColor()
getColor()
test.getColor()
```

green, blue

- 因为alert()内部含有this，因此会找全局变量

### 2.微任务 & 宏任务

```javascript
console.log(1);
let a = setTimeout( () => console.log(2) )
console.log(3)
Promise.resolve(4).then(b => {
	console.log(b)
	clearTimeout(a)
})
console.log(5)
```

1354

- 先执行同步135，定时器为宏任务，Promise.then()为微任务，因此先执行微任务，微任务中清除了定时器，所以不会有2，其中宏任务和微任务均属于异步
    - 宏任务：消息队列中等待被主线程执行的事件，宏任务执行会重新创建栈，宏任务执行结束后，栈也会随之销毁【】
    - 微任务：需要被异步执行的函数。【】
      总的执行顺序：宏任务中的同步任务 -> 微任务当中的同步任务 ->微任务中的异步任务 -> 宏任务中的异步任务

## 2023.01.04

### 1.数组length

- 数组length属性总比数组最后一个元素的下标大1
- 对于后续更改length属性
    - 如果新值 < 当前值，数组将被截断
    - 如果新值 > 当前值，数组将被扩大，扩大的元素值为underfined

### 2. let作用域

```javascript
for(let i=0;i<2;i++){
    setTimeout(function(){
    console.log(i)
    },100);
}
for(var i=0;i<2;i++){
    setTimeout(function(){
        console.log(i)
    },100);
}
```

0122

- let 有自己的作用域块，在for循环中满足每次i输出一次
- var没有自己的作用域，又因为settimeout是异步的，所以当异步任务开始前，i已经是2了

### 3.indexOf() & includes()

- indexOf 返回索引值，用于检查是否有该值，没有的话返回-1
- includes 用于检查数组中是否有某个元素值

### 4.集合A -> 数组转化方式

- Array.from(A)
- [].slice.apply(A)
    - 从已有的数组中返回选定的元素，该方法不会影响原数组
    - 如果想删除数组中元素，需要使用splice，或者pop 或者shift等
- [...A]
    - ...展开运算符
- [].map.call(A, o => o)

## 2023.01.05

### 1.parentNode

- 返回指定节点再DOM树中的父节点
- 一个元素节点的父节点可能是元素element节点，可能是文档Document节点，也可能是文档碎片DcoumentFragment节点

```javascript
//删除指定节点
if(node.parentNode) {
	node.parentNode.removeChild(node)
} //如果该节点刚刚被建立，没有被插入到DOM树中，该节点的parentNode也返回null
```

## 2023.01.06

### 1.闭包

```javascript
function Foo(){
	var i = 0;
	return function(){
		doucment.write(i++)
	}
}
var f1 = Foo()
f2 = Foo()
f1();
f1();
f2()
```

010
第二次调用f1()，因为return返回了1，所以形成了闭包，相应的作用域链也会保存在内存中。

### 2.共享原型链，堆地址相同

```javascript
function Father(age){
    this.age = age
}
function Son(age){
    Father.call(this);
}
Son.prototype = Father.prototype;
Father.prototype.getAge = function(){console.log(40);}
Son.prototype.getAge = function(){console.log(18);}
var father = new Father(40);
var son = new Son(18);
son.getAge();
father.getAge();
```

18 18
因为有两个原型链的地址相同，所以以最终赋值结果输出。

## 2023.01.07

### 1.

```javascript
//A
var res = arr.filter((val1,val2)=>{
	return val1 > 10;
})
console.log(res); 
//B
var res = arr.filter((val1,val2)=>{
	return val1 > 10;
})
console.log(res);
//C
var res = arr.filter((val1,val2)=>{
	return val1 > 10;
})
console.log(res);
//D
var res = arr.map((val1,val2)=>{
	return val1 > 10;
})
console.log(res);
```

- filter用于筛选满足要求的数组元素，并返回新的数组，所以结果[20,12]
- some用于监测是否有满足条件的元素，存在则返回true，所以返回一个true
- every用于监测所有元素是否都符合条件，都符合才是true，所以结果false
- map用于对元素单独判断，返回新的数组，所以结果[false,true,false,true,false]

## 2023.01.08

### 1.对象类型

- typeof
  - 返回一个字符串，表示未经计算的操作数的类型，下面是typeof可能返回值

| 类型         | 结果         |
| ------------ | ------------ |
| undefined    | "underfined" |
| Null         | "object"     |
| Boolean      | "boolean"    |
| Number       | "number"     |
| String       | "string"     |
| Bigint       | "bigint"     |
| Symbol       | "symbol"     |
| Function     | "function"   |
| 其他任何对象 | "object"     |

- instance of
  - 用于检测构造函数的prototype是否出现在某个实例对象的原型链上
  - 如 object1 instanceof constructor 或者 [] instanceof Array

### 2. ||  && 运算符

- ||
  - 如果第一个结果是true，则返回该操作数的值；如果false则一直迭代，直到存在结果为真，否则返回最后一个元素
  - 如 let flag1 = null || underfined，因为都是false，所以返回最后的undefined
- &&
  - 如果有一个值为false，则返回该值；当所有值都为true，则返回最后一个操作数
  - 如let flag2 = null && undefined，因为第一个就是false，所以返回第一个null

## 2023.01.09

### 1.appendChild() & removeChild()

- appendChild()
  -  在该元素的最后添加一个子节点

## 2023.01.10

### 1.闭包、函数和变量提升练习题

```javascript
var a = 10; 
(function a() {
    a = 20;
    console.log(a); 
})()
```

会输出函数a的内容

- 首先是变量提升，a = undefined
- 之后由于函数提升优先级高于变量赋值，所以先a被赋值函数
- 闭包函数，所以内部即刻输出
  - 其中 a = 20，意思是将整个函数赋值为20，并不是新的变量a
- 输出a的函数体后，最后才是全局a再次赋值为10

## 2023.01.11

### 1.隐式转换

```javascript
var foo = "10" + 3 - "1"
console.log(foo) //102
```

- '+'
  - 第一种含义是字符串拼接，第二种是加减法
  - 上述代码块，如果一个是字符串，默认会转成字符串
- '-'
  - 只有一个加减法的含义，因此是不是字符串了
- 补充underfined会被转为NAN，null会转为0，此外0也可以作为除数

### 2.变量、函数执行顺序

```javascript
var a = 2;
function fn(){
	b()
	return ;
	var a = 1;
	function b(){
		console.log(a);
	}
}
fn()
```

undefined

- 虽然return可以终止函数，但如果后面有变量 和 函数声明，仍然存在变量提升和函数提升
- 当调用函数b时，需要查找a的值。根据作用域链查找规则，要在函数内部查找已经声明的局部变量a。但虽然a变量声明提高了，但是值却没有赋值，所以是underfined

### 3.变量回收机制

```javascript
var i = 1;
var i = 2;
var add = function() {
	var i = 0;
	return function(){
		i++;
		console.log(i)
	}
}();
add();
```

上述代码执行后，有 3 个变量没有被回收

- 首先全局变量 i 没有回收，其中第二行的var 没有作用，只是做了更新赋值
- 第二个函数add变量
- add函数内部变量i，因为该闭包中的局部变量倍 return的结果作用域引用，所以没有被回收

### 4.页面跳转

- window.history.forward()  //下一个页面
- window.history.back()  //上一个页面
- window.history.go()  //相对当前页面到指定页面
  - go(1)  //相当于forward
  - go(-1)  //相当于back

## 2023.01.12

### 1.符号解释

- '*' 表示出现零到多次
- '+'表示出现1到多次
- '？'表示出现0词次或者1次
- '.'表示除了\n意外的任意字符
- {n}表示出现n次
- {n,}表示出现n到多次
- {n,m}表示出现n到m次

### 2.search

```javascript
let str = 'what is this'
let x = str.search('is')
console.log(x) //5
```

search从左边第一个字符开始查找，此时下标为0. is中的i在第六个字符位置，下标是5，所以输出 5

## 2023.01.13

### 1.noscript

noscript标签用来定义在脚本未被执行时候的替代内容

```javascript
<body>  
...
  ...
  <script type="text/javascript"> <!--
 document.write("Hello World!")
 //--> </script><noscript>Your browser does not support JavaScript!</noscript>...
  ...
</body>
```

## 2023.01.14

### 1.函数声明和函数表达式

```javascript
function sum(a,b){
	return a + b
} //函数声明
```

```javascript
var sum = function(a,b){
	return a + b
} //函数表达式
```

### 2.window小窗口

- window.confirm()  确认框
- window.alert()  警示框
- window.prompt()  对话框
- window.open() 打开新的窗口

### 3.hasOwnProperty

- 该方法用来检测指定的属性是否在当前对象的实例中

## 2023.01.15

### 1.parseFloat() & toFixed() & Math.round & toPrecision()

- parseFloat()  返回一个浮点数
  - parseFloat(0.2 + 0.4)   //0.600000000001
- toFixed()  把数字转换为字符串，结果的小数点后有指定位数的数字
  - parseFloat((0.2 + 0.4).toFixed(1)) //'0.6'
- Math.round  返回四舍五入的整数
  - Math.round(0.2 + 0.4)  //1
- toPrecision  把数字格式化为指定的长度
  - parseFloat((0.2 + 0.6).toPrecision(1)) //0.8

