# 前端面试题

## 2023.01.16

### 1.类方法 和 对象方法

```javascript
class Dog{
	static dog(){
		console.log(this)
	}
	bark(){
		console.log('bark')
	}
}
```

- dog方法为静态方法
    - 静态方法不能使用实例对象调用，所以this不会指向实例对象
    - 静态方法只能通过类调用
- bark为实例方法
    - 实例方法只能实例对象调用
- 因为类静态方法 和 实例方法调用者不同，所以这两种方法名可以同名（尽量不要）

## 2023.01.17

### 1.arr.forEach()

```javascript
var arr = [2,1,3,5,9];
var count = 0;
arr.forEach((val1,val2)=>{
	count++;
	if(count % 3 == 0){
		return;
	}
	console.log(val1);
})
```

2159

- arrForEach()循环遍历数组，参数是==一个函数==，迭代以此数组，函数执行以此。
- 函数内部有return，只是相当于当前迭代结束，不会影响下次迭代

### 2.NaN

NaN无论和谁比较（包括自己），都是false

### 3.process.nextTick() & pormise.then()

```javascript
Promise.resolve()
.then(() => {
  console.log('p1');
})
.then(() => {
  console.log('p2');
})
process.nextTick(() => {
  console.log('n1');
  process.nextTick(() => {
    console.log('n2');
  })
})
```

n1,n2,p1,p2
两者都属于微任务，只是nextTick优先级更高

## 2023.01.18

### 1.原始值（简单类型） 和 引用值（对象类型）

原始值：存储栈中

- undefined
- null
- boolean
- number
- string
  引用值：存储堆中

### 2.Math.max

```javascript
//第一种方式
Math.max(arr[0],arr[1],arr[2],arr[3])

//第二种方式
Math.max.call(Math,arr[0],arr[1],arr[2],arr[3])

//第三种方式
Math.max.apply(Math,arr)
```

- call 第一个参数是调用对象，第二个至后可以直接讲数组项带入
- apply第一个参数是调用对象，第二个参数是数组

## 2023.01.19

### 1.arr.filter/some/every/map

```javascript
arr = [2,20,3,12,9]
let res = arr.filter((val1,val2) => {
	return val1 > 10
})
true
```

- filter用于筛选出符合满足要求的数组，返回一个新的数组 [20,12]
- some用于监测数组是否有满足条件的元素，有则返回true，否则false;true
- every用于监测数组是否都符合条件，都满足才返回true  ;false
- map用于对每个元素进行单独判断，返回一个新数组[f,t,f,t,f]

### 2.加法 、字符串合并与一元操作符

```javascript
console.log(1+ "2"+"2");
console.log(1+ +"2"+"2");
console.log("A"- "B"+"2");
console.log("A"- "B"+2);
```

122; 做加法有双引号默认是字符串合并，所以122
32；+ 2是一元+操作符，所以会变成3 + “2”，所以32
NaN2；做减法只能是加减运算，所以转Number，结果NaN，加上字符串2
NaN；NaN + 任何都是 NaN

## 2023.01.20

### 1.Object.is() 和 ===

Object.is() 和 === 都是判断两个数是否严格相等
去呗在于 NaN 和 +0 和 -0的区别

### 2.reflow回流 和 repaint重绘

- reflow：几乎无法避免，比如页面中树桩目录的折叠、展开、都回引起整个页面的重新渲染，如以下几种情况会导致reflow发生
    - 改变窗口大小
    - 改变文字大小
    - 内容改变
    - 激活伪类（如hover）
    - 操作class属性
    - 脚本操作DOM
    - 计算offsetWidth 和 offsetHeight
    - 设置style属性
- repaint：一般只是改变元素的背景颜色、文字颜色、边框颜色等会触发

## 2023.01.21

### 1.Math

- Math.ceil()向上取整，即总是将数值向上舍入为最接近的整数
- Math.floor()向下取整，总是将数值向下舍入为最接近的整数
- Math.round()标准取整，四舍五入

## 2023.01.22

### 1.新年快乐
​:rabbit: ​:rabbit:

## 2023.01.25

### 1.undefined 和 null

- underfined 和 null与任何有意义的值比较返回都是false
- null与underfined之间互相比较是true

## 2023.01.26

### 1.tagName

- tagName()用于返回当前标签的名字

### 2.例题

```html
<button>点击</button>
<script> function a(){
    console.log(1);
    return function b(){
        console.log(2);
}
}
var btn = document.querySelector('button');
btn.onclick = a(); ...①
btn.onclick = a; ...② </script>
```

- 注释①
    - 不会立即执行a()，点击按钮输出1，return语句不会执行
- 注释②
    - a()方法即刻执行，直接输出1，点击事件对应return内方法，当点击触发则输出2

## 2023.01.27

### 1.遍历对象属性

```javascript
var my_data={a:’Ape’, b:’Banana’, c:’Citronella’};
```

for(var key in my_data){}

### 2.hasOwnProperty

用于判断一个对象是否有给出指定名称的属性，但无法检查该对象的原型链是否有该指定属性。

## 2023.01.28

### 1.包装类的对象

```javascript
1 in Object(1.0).constructor;
Number[1] = 123;
1 in Object(1.0).constructor;
```

-   constructor 是构造函数属性。它是谁的属性？它**是原型属性 prototype 所指向的那个对象的属性**。
-   Object(1.0).constructor 的原型是 Number 对象。
-   Number 对象本身可作为构造函数，所以 Object(1.0).constructor 就是 Number 对象本身。

### 2.Cookie

- Cookie是不超过4KB的小型文本数据，由名称Name，值Value和其他用于控制Cookie有效性、安全性、使用范围的可选属性组成；
- Cookie的过期时间如果不做设置，默认浏览器关闭时结束

### 3.

```javascript
var o = {
	age: 18,
 	a: {
		fn: function(){
			console.log(this.age);
		}
	}
}
o.a.fn();   //undefined
```

```javascript
var person = {
	sex:"女",
	fn: function(){
		console.log(this.sex);
	}
}
person.fn()  //女
```

```javascript
var a = 'w' 
let obj = {
  a: 'o',
  print: function() {
    console.log(this.a);
  },
  print2: () => { 
    console.log(this.a);
  }
}
let p = obj.print;
let p2 = obj.print2;
obj.print();
obj.print2();
p();
p2();   // o w w w
```

## 2023.01.29

### 1.例题

```javascript
let obj = {
  num1: 117
}
let res = obj;
obj.child = obj = { num2: 935 };
var x = y = res.child.num2;
console.log(obj.child);  //undefined
console.log(res.num1);  //117
console.log(y);  //935
```

![输入图片描述](http://liusihu.gitee.io/nutcloud/202301291.jpg)
![输入图片描述](http://liusihu.gitee.io/nutcloud/202301292.jpg)
![输入图片描述](http://liusihu.gitee.io/nutcloud/202301293.jpg)
![输入图片描述](http://liusihu.gitee.io/nutcloud/202301294.jpg)

### 2.false值

- undefined
- null
- +0
- -0
- NaN

## 2023.01.30

### 无
